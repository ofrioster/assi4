

enum StompCommand {
    // client-commands
    CONNECT, SEND, SUBSCRIBE, UNSUBSCRIBE, LOGIN, COMMIT, ABORT, ACK, DISCONNECT,

    // server-commands
    CONNECTED, MESSAGE, RECEIPT, ERROR, DISCONNECTED
}